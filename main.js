const app = Vue.createApp({
    data() {
        return {
            product: 'Socks',
            description: 'Nice warm socks to keep you cozy this winter!'
        }
    }
})
// Did this using the ES6 shorthand 
